<div class="card text-center mx-auto mb-5" style="width: 32rem; background-color: #626263;">
  <div class="card-header">
    <center><h2>Your Poems</h2></center>
  </div>
</div>
<% @poems.each do |poem| %>
  <%= render partial: 'showcard', locals: {poem: poem, category: poem.category_name} %>
<% end %>
