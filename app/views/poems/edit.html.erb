<%= render partial: 'layouts/errors', locals: {object: @poem} %>
<div class="card text-center mx-auto mb-5" style="width: 37rem;">
  <div class="card-header">
    <h2><u>Edit <%= @poem.title %></u></h2>
  </div>
  <%= render partial: 'form', locals: {poem: @poem} %>
</div>
