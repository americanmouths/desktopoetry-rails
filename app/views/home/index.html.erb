<% if current_user %>
  <center><h2 class="welcome">Welcome <%= current_user.name %>!</h2></center>
<% else %>
  <div class="card text-center mx-auto mb-5" style="width: 30rem;">
    <div class="card-header">
    </div>
    <div class="card-body">
      <h5 class="card-title">Welcome to <h2 class="title">desktopoetry</h2></h5>
      <p class="card-text">An app that allows poets to easily create & manage their poetry.</p>
    </div>
    <div class="card-footer">
    </div>
  </div>
<% end %>
